name: xchain_crypto
description: A Dart package for cryptographic operations, including encryption, decryption, and keystore management. Lib derives from xchainjs-crypto.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  bip39: ^1.0.6
  crypto: ^3.0.6
  cryptography: ^2.7.0
  pointycastle: ^3.9.1
  uuid: ^4.5.1
  # path: ^1.8.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
  dotenv: ^4.2.0

resolution: workspace
